package com.timothee.corrado

import org.junit.Test

class ArticleModelTests {
/*
    @Test
    fun getValidArticleText() {

        val pool = ConnectionPool("jdcb:h2:mem:testd;MODE=MYSQL", null, null)
        val model = ArticleMysqlModel(pool)

        pool.userConnection { connection ->
            connection.prepareStatement("CREATE TABLE article (\n" +
                "   id INT NOT NULL AUTO_INCREMENT,\n" +
                "   text LONGTEXT NOT NULL,\n" +
                "   PRIMARY KEY (`id`));"
            ).use

        }

    }
    */
}